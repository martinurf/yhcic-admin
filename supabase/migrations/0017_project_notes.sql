-- Optional internal scratch notes, separate from the public-facing
-- "body" description — for things like sourcing, open questions, or
-- assumptions that shouldn't show up if the project gets published.

alter table projects add column notes text;
